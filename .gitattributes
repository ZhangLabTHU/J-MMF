# ── Git LFS (if needed in future) ──
# *.eam filter=lfs diff=lfs merge=lfs -text

# ── Export-ignore for `git archive` (used by GitHub Releases) ──
.gitattributes export-ignore
.gitignore export-ignore
.git export-ignore
Makefile export-ignore

# ── Line endings ──
*.py text eol=lf
*.md text eol=lf
*.eam text eol=lf
*.traj binary
*.pkl binary
